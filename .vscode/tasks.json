{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "& '${workspaceFolder}/.venv/Scripts/Activate.ps1'; pyinstaller main.py --collect-all bpy --collect-all utils -n QuVery-Core -y",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            }
        }
    ]
}
